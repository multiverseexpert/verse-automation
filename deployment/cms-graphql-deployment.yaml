apiVersion: apps/v1
kind: Deployment
metadata:
  name: cms-graphql-deployment
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: cms-graphql-deployment
  template:
    metadata:
      labels:
        app: cms-graphql-deployment
    spec:
      containers:
        - image: mexverse/verse-cms-graphql:latest
          name: cms-graphql-deployment
          ports:
            - containerPort: 80
          resources:
            requests:
              memory: "64Mi"
              cpu: "250m"
            limits:
              memory: "128Mi"
              cpu: "500m"
